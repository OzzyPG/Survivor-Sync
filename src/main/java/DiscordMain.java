import com.jagrosh.jdautilities.command.CommandClientBuilder;import net.dv8tion.jda.api.JDA;import net.dv8tion.jda.api.JDABuilder;import net.dv8tion.jda.api.entities.Activity;import net.dv8tion.jda.api.requests.GatewayIntent;import java.sql.Connection;import java.sql.DriverManager;import java.sql.SQLException;public class DiscordMain {    public static void main(String[] args) throws Exception {        //REQUIRES SQL DATABASE WITH TABLE "MasterList", VALUES "DiscordID, RoleID, OrgC (VARCHAR, 100LENGTH)        //DO NOT REMOVE BRANDING        //EDIT CODE AT YOUR OWN RISK        //DO NOT DISTRIBUTE WITHOUT PERMISSION FROM OZZY        //BotSetup :)       CommandClientBuilder commandClientBuilder = new CommandClientBuilder();       commandClientBuilder.setPrefix("$!");       commandClientBuilder.useDefaultGame();       commandClientBuilder.setOwnerId("183951503726346240");       //JDA API        JDA jda = JDABuilder.createDefault("BOT TOKEN GOES HERE").enableIntents(GatewayIntent.GUILD_MEMBERS).build();            try {                Thread.sleep(5000);            } catch (Exception ex) {            }        jda.getPresence().setActivity(Activity.playing("Bot made by Ozzy"));        jda.addEventListener(new RoleUtils());        }        //Static Connection Info        public static Connection connection() throws SQLException {            return DriverManager.getConnection("SQL LOGIN SHIT GOES HERE");        }    }